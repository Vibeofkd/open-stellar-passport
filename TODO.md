# TODO

## Passport analytics endpoint (aggregate across all passports)

- [x] Add analytics computation method to `frontend/src/lib/passport-store.ts` (counts: active, revoked, expired, expiring)
- [x] Add new API route `frontend/app/api/protocol/passport/analytics/route.ts`

- [ ] Add unit tests for the store analytics method
- [x] Add route tests for the new endpoint
- [ ] Run frontend/unit tests to confirm everything passes


